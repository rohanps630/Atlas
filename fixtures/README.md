# fixtures/

Tiny sample repos used to test the extractors and core **without ever needing client code**
(ADR 0003). Add a minimal multi-repo fixture (one FE + one BE) in Phase 2 to exercise
cross-repo contract linking and the `external` node path.
