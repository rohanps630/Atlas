# mcp/  (Phase 3)

An MCP server that serves the structured map to coding agents (Claude Code, Kiro) so they
can query it on demand instead of re-deriving it by searching.

Planned tools: `context`, `callers`, `impact`, `endpoints`. Read-only over the data store.
