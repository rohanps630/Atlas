# The tree-sitter grammar packages (swift/kotlin) declare slightly different
# tree-sitter peer ranges; they are ABI-compatible at runtime (see ADR 0008).
# This lets a clean `npm install` succeed without --legacy-peer-deps each time.
legacy-peer-deps=true
